# repositories/

`tracker.repository.js`, `clan.repository.js`, `auth.repository.js`,
`hearts.repository.js`, `timers.repository.js`, `shining.repository.js`,
`admin.repository.js` — см. историю выше.
`anomaly.repository.js` — SQL для Аномальных прорывов (per-account,
не per-clan — отдельная таблица `user_anomaly`).

Все роуты теперь на этом слое.
