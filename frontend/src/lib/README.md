# lib/

Тонкие обёртки над внешним миром, без бизнес-логики:
apiClient.ts (сейчас это utils/api.js), socketClient.ts (сейчас часть
hooks/useSocket.js), sound.ts (сейчас utils/sound.js или похожее).

Пока пусто — существующие utils/hooks продолжают работать как есть,
переносятся сюда при миграции соответствующего модуля.
