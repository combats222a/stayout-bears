# features/

Доменные модули (auth, clan, tracker, hearts, timers, shining, captures,
achievements, anomaly, admin, public) — каждый со своими api.ts, store.ts,
components/ и страницей.

Пока пусто. Первый модуль, который сюда переедет — `tracker/`
(общий движок для Bears + Draugs, сейчас это два почти идентичных файла
в `pages/BearsPage.jsx` и `pages/DraugsPage.jsx`).
