# features/admin/

`AdminPage.jsx` — перенесена как есть, без пропсов. Сохранил как есть
её существующий баг: импортирует `formatTime`/`getTimeLeft` из
`utils/bears.js`, которых там нет (см. ARCHITECTURE.md → «Известные
существующие проблемы») — не трогал специально, это не входило в задачу
переноса.
