# features/timers/

`TimersPage.jsx` — перенесена как есть из `pages/`, интерфейс не менялся
(`user`, `onLoginClick`). Полностью самодостаточна — не делит состояние
ни с одной другой страницей (в отличие от clan/hearts/members), поэтому
это самый простой перенос из всех: только пути импортов.
