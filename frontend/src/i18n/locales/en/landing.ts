import type { DeepValuesToString } from '../../types';
import type ruLanding from '../ru/landing';

const landing: DeepValuesToString<typeof ruLanding> = {
  ctaText: 'Want to track bears, Shining Mountain, and your clan timers too?',
};

export default landing;
